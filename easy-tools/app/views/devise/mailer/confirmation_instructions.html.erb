<div class="container mt-4">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card shadow rounded">
        <div class="card-body">
          <h2 class="text-center mb-4">Olá <%= @resource.email %>!</h2>

          <p class="text-center">
            Você pode confirmar o e-mail da sua conta através do link abaixo:
          </p>

          <div class="text-center mt-4">
            <%= link_to 'Confirm My Account', confirmation_url(@resource, confirmation_token: @token), class: "btn btn-primary" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
