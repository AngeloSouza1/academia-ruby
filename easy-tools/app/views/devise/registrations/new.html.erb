<div class="container">
  <div class="row justify-content-center mt-5">
    <div class="col-md-6">
      <div class="card shadow rounded">
        <div class="card-body">
          <h2 class="text-center mb-4">Inscrever-se</h2>

          <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: 'form' }) do |f| %>
            <%= f.error_notification %>

            <div class="mb-3">
              <%= f.input :email,
                          required: true,
                          autofocus: true,
                          input_html: { autocomplete: "email", class: 'form-control', placeholder: 'Email' } %>
              <%= f.input :password,
                          required: true,
                          hint: ("#{@minimum_password_length} mínimo de caracteres" if @minimum_password_length),
                          input_html: { autocomplete: "Nova Senha", class: 'form-control', placeholder: 'Senha' } %>
              <%= f.input :password_confirmation,
                          required: true,
                          input_html: { autocomplete: "Nova Senha", class: 'form-control', placeholder: 'Confirme sua senha' } %>
            </div>

            <div class="text-center">
              <%= f.button :submit, "Inscrever-se", class: "btn btn-primary btn-lg btn-rounded" %>
            </div>
          <% end %>

          <%= render "devise/shared/links" %>
        </div>
      </div>
    </div>
  </div>
</div>
